#!/usr/bin/sh

# Below are the missing dependencies that should be met to enable using
# `latexindent` to format `.tex` files,
#

apt-get install libyaml-tiny-perl
apt-get install libfile-homedir-perl
apt-get install libunicode-utf8-perl
apt-get install libunicode-linebreak-perl
